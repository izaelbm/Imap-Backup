#By Izael Magalhaes
#Backup and Restore IMAP Emails
1. Install dependencies
  #sudo apt update -y
  #sudo apt install isync mbsync -y

2. Para realizar o restore, precisa criar uma pasta Backup na caixa de email que ira receber os emails  
